<%= form_with(model: weekly_collection) do |form| %>
  <% if weekly_collection.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(weekly_collection.errors.count, "error") %> prohibited this weekly_collection from being saved:</h2>

      <ul>
        <% weekly_collection.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="form-group">
    <%= form.label :week_date, style: "display: block" %>
    <%= form.date_field :week_date, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :amount, style: "display: block" %>
    <%= form.number_field :amount, step: '0.01', class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.submit class: "btn btn-success" %>
  </div>
<% end %>
