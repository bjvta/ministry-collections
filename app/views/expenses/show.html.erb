<p style="color: green"><%= notice %></p>

<%= render @expense %>

<div>
  <%= link_to "Edit this expense", edit_expense_path(@expense), class: 'btn btn-warning' %> |
  <%= link_to "Back to expenses", expenses_path, class: 'btn btn-info' %>
  </br>
  </br>
  </br>
  <%= button_to "Destroy this expense", @expense, method: :delete, class: 'btn btn-danger' %>
</div>
